project:
  type: default
  output-dir: _site
  resources:
    - index.html

# Single reveal.js presentation. `Pre-GenAI-in-Pharma-2026.qmd` renders to
# `_site/Pre-GenAI-in-Pharma-2026.html`. `index.html` is copied verbatim and
# redirects the repo-root URL to that page. The GitHub Pages workflow publishes
# the whole `_site/` directory.
