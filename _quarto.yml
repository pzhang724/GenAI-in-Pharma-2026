project:
  type: default
  output-dir: _site
  render:
    - Pre-GenAI-in-Pharma-2026.qmd

# Single reveal.js presentation. `Pre-GenAI-in-Pharma-2026.qmd` sets
# `output-file: index.html`, so it renders to `_site/index.html` and is
# served at the site root. The GitHub Pages workflow publishes `_site/`.
