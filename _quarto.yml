project:
  type: default
  output-dir: _site

# Single reveal.js presentation. `Pre-GenAI-in-Pharma-2026.qmd` renders to
# `_site/Pre-GenAI-in-Pharma-2026.html`. The GitHub Pages workflow publishes
# the whole `_site/` directory; link the deck at that path directly.
